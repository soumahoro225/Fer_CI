import { NextResponse } from "next/server";
import { createClient } from "../../../lib/supabase/server";
export async function GET(){const s=await createClient();const{data:auth}=await s.auth.getUser();if(!auth.user)return NextResponse.json({error:"Authentification requise"},{status:401});const{data,error}=await s.from("incidents").select("id,reference,title,category,location,severity,status,latitude,longitude,created_at").order("created_at",{ascending:false}).limit(100);if(error){console.error("incidents.select",error);return NextResponse.json({error:"Lecture impossible"},{status:500})}return NextResponse.json({incidents:data})}
export async function POST(request:Request){const s=await createClient();const{data:auth}=await s.auth.getUser();if(!auth.user)return NextResponse.json({error:"Authentification requise"},{status:401});let b:Record<string,unknown>;try{b=await request.json()}catch{return NextResponse.json({error:"Corps JSON invalide"},{status:400})}const title=String(b.title||"").trim(),location=String(b.location||"").trim(),observations=String(b.observations||"").trim(),category=String(b.category||"Voirie"),severity=String(b.severity||"Modérée"),latitude=Number(b.latitude),longitude=Number(b.longitude);if(!title||title.length>160||!location||location.length>240||observations.length>2000||!["Voirie","Feux","Accotement","Ouvrage","Bac","Péage / pesage"].includes(category)||!["Critique","Élevée","Modérée"].includes(severity)||!Number.isFinite(latitude)||latitude < -90||latitude > 90||!Number.isFinite(longitude)||longitude < -180||longitude > 180)return NextResponse.json({error:"Données de signalement invalides"},{status:400});const{data,error}=await s.from("incidents").insert({reference:`FER-${crypto.randomUUID().slice(0,8).toUpperCase()}`,title,category,location,observations,severity,latitude,longitude,created_by:auth.user.id}).select().single();if(error){console.error("incidents.insert",error);return NextResponse.json({error:"Enregistrement impossible"},{status:500})}return NextResponse.json({incident:data},{status:201})}
